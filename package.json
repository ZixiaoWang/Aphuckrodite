{
  "name": "aphuckrodite",
  "targets": {
    "default": {
      "includeNodeModules": true
    }
  },
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/css": "^0.0.33",
    "@types/sass": "^1.16.1",
    "aphrodite": "^2.4.0",
    "esbuild": "^0.12.26",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "css": "^3.0.0",
    "sass": "^1.39.2"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --metafile=esbuild.json --minify --outfile=dist/index.js --platform=node --external:fsevents --tree-shaking=ignore-annotations"
  }
}
